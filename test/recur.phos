fn fact(x: i64) -> i64 {
    if (x == 1) return 1;
    return x * fact(x - 1);
}

fn fib(x: i64) -> i64 {
    if (x <= 1) return x;
    else return fib(x - 1) + fib(x - 2);
}

print(fact(5));
print(fib(6));
