fn fact(x: int) -> int {
    if (x == 1) return 1;
    return x * fact(x -1);
}

fn fib(x: int) -> int {
    if (x <= 1) return x;
    else return fib(x - 1) + fib(x - 2);
}

print fact(5);
print fib(6);
